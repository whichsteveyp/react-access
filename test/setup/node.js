// Require this file to set up your tests that you run in Node (as opposed
// to tests that you might run in an actual browser)

// The goal here is to set up Node similarly to if these tests were being
// set up in the browser, so we set "browser globals"
global.assert = require('assert');
